import { View, Text } from 'react-native';
import React, { FC } from 'react';

const AbsenDetailScreen: FC = () => {
	return (
		<View>
			<Text>AbsenDetailScreen</Text>
		</View>
	);
};

export default AbsenDetailScreen;
